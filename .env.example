# No personal GitHub token is accepted or required.
# Runtime settings live in wrangler.jsonc. AI interpretation is off by default.
# Optional external Playwright package path for local visual QA only:
# PTRUST_QA_MODULES=/path/to/node_modules
